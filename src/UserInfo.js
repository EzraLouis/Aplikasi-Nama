import React from 'react';

function UserInfo({ name, nim }) {
  return (
    <div className="text-center">
      <p className="text-lg font-semibold">
        Identitas Mahasiswa
      </p>
    </div>
  );
}

export default UserInfo;
